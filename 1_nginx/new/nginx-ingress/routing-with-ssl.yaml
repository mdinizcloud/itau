apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service-a
  namespace: default
  annotations:
    # nginx.ingress.kubernetes.io/auth-type: basic
    # nginx.ingress.kubernetes.io/auth-secret: server-a-secret
    # nginx.ingress.kubernetes.io/auth-secret-type: auth-file
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - services.cybertron.com
    secretName: main-ssl  

  rules:
  - host: services.cybertron.com
    http:
      paths:
      - path: /service-a(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: service-a
            port:
              number: 80
  # tls:
  # - hosts:
  #   - services.cybertron.com
  #   secretName: main-ssl

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service-b
  namespace: default  
  annotations:
    # nginx.ingress.kubernetes.io/auth-type: basic
    # nginx.ingress.kubernetes.io/auth-secret: server-a-secret
    # nginx.ingress.kubernetes.io/auth-secret-type: auth-file  
    nginx.ingress.kubernetes.io/use-regex: "true"  
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - services.cybertron.com
    secretName: main-ssl  
  rules:
  - host: services.cybertron.com
    http:
      paths:
      - path: /service-b(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: service-b
            port:
              number: 80
  # tls:
  # - hosts:
  #   - services.cybertron.com
  #   secretName: main-ssl              
---